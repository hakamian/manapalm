### 🔒 اصلاح سیستم احراز هویت و ذخیره‌سازی پروفایل (2026-01-22)

**اقدامات انجام شده:**
1.  **RTL OTP Fix:**
    *   اصلاح جهت باکس‌های دریافت کد تایید در `AuthModal.tsx`. کدها اکنون از **راست به چپ** وارد می‌شوند.
    *   منطق جابجایی فوکوس و کلیدهای فلش برای حالت RTL بهینه‌سازی شد.
2.  **User Identity Persistence (Refactor):**
    *   در `AppContext.tsx` اولویت لود دیتا تغییر کرد: اپلیکیشن ابتدا دیتای غنی‌شده را از دیتابیس (SSOT) لود می‌کند.
    *   این کار مانع از بازنویسی نام انتخاب شده توسط کاربر با اطلاعات اولیه گوگل (Google Name) می‌شود.
    *   بهبود منطق `loadInit` برای بازیابی ID کاربر از سشن Supabase در صورت خالی بودن `localStorage`.

**وضعیت جاری:**
- ⚠️ **ذخیره اطلاعات کاربری:** منطق لود اولیه اصلاح شده اما طبق گزارش کاربر، هنوز در برخی سناریوها ذخیره‌سازی نهایی با مشکل مواجه است. نیاز به بررسی عمیق‌تر API Route و وضعیت RLS دیتابیس در جلسه بعد.

**گام‌های بعدی (فردا):**
1.  دیباگ نهایی API `update-user-v2.js` و اطمینان از صحت `upsert` در جدول `profiles`.
2.  تست نهایی تغییر نام و ماندگاری آن در حالت‌های مختلف.

---

**🌴 این حافظه زنده است و با هر پیشرفت به‌روزرسانی می‌شود.**
