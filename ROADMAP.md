
# نقشه راه فنی نخلستان معنا (مسیر تبدیل MVP به Production)

این سند وضعیت فعلی پروژه و گام‌های ضروری برای لانچ موفق برای ۱۰۰۰ کاربر و بیشتر را تشریح می‌کند.

## 🚨 وضعیت فعلی: نسخه نمایشی پیشرفته (Advanced Prototype)
- **نسخه:** 0.1.0
- **نوع معماری:** Client-Side Only (SPA) با شبیه‌سازی کامل Backend.
- **ذخیره‌سازی داده:** `dbAdapter` (در حال حاضر روی داده‌های ساختگی/LocalStorage تنظیم شده است).
- **امنیت:** متوسط (نیاز به انتقال کلیدها به سرور برای پروداکشن).
- **وضعیت:** تمام فیچرهای اصلی (فروشگاه، آکادمی، داشبورد، ادمین) کدنویسی شده‌اند. آماده برای اتصال به دیتابیس واقعی.

---

## 🛠 فاز ۱: زیرساخت و دیتابیس (هفته جاری)
*هدف: حفظ اطلاعات کاربران و امنیت سیستم*

### ۱. اتصال واقعی Supabase
- [ ] اجرای اسکریپت‌های SQL برای ساخت جداول (`users`, `orders`, `content`, `logs`).
- [ ] به‌روزرسانی `dbAdapter.ts` برای خواندن/نوشتن از Supabase به جای Mock Data.
- [ ] تنظیمات Authentication (Google & Phone Auth).

### ۲. ایمن‌سازی هوش مصنوعی (Backend Proxy)
- [ ] دیپلوی کردن فایل `api/proxy.js` روی Vercel یا یک سرور Node.js.
- [ ] انتقال `GEMINI_API_KEY` به متغیرهای محیطی سرور (Environment Variables).

### ۳. درگاه پرداخت
- [ ] پیاده‌سازی کامل فلو پرداخت با زرین‌پال (تست در محیط Sandbox انجام شده).
- [ ] ایجاد Webhook برای تایید تراکنش‌ها.

---

## 🚀 فاز ۲: بهینه‌سازی و لانچ (ماه آینده)

### ۱. سئو و پرفورمنس
- [x] تنظیم متا تگ‌ها و `sitemap.xml`.
- [ ] بهینه‌سازی تصاویر (استفاده از فرمت WebP).
- [ ] پیاده‌سازی Lazy Loading برای کامپوننت‌های سنگین (انجام شده برای بخش‌های اصلی).

### ۲. تست‌های نهایی
- [ ] تست کامل فلو خرید توسط کاربر واقعی.
- [ ] تست سناریوهای هوش مصنوعی (دقت و سرعت پاسخگویی).

---

**تایید شده توسط:** مانا (مدیر فنی)
