# 🧪 گزارش تست Production - manapalm.com

**تاریخ:** 2025-12-11 13:22  
**وضعیت:** در حال تست

---

## ✅ تست 1: دسترسی به سایت

**نتیجه:** ✅ موفق
- سایت در https://manapalm.com بارگذاری شد
- صفحه اصلی نمایش داده شد
- عنوان صفحه: "خانه | نخلستان معنا"

---

## 🔄 تست 2: Environment Variables

**روش تست دستی:**

1. سایت را باز کنید: https://manapalm.com
2. F12 را بزنید (Developer Tools)
3. به تب **Console** بروید
4. این کد را کپی و اجرا کنید:

```javascript
// تست اتصال Supabase
if (window.supabase) {
  console.log('✅ Supabase Client: موجود است');
} else {
  console.log('❌ Supabase Client: موجود نیست');
}

// تست Environment Variables (از طریق dbAdapter)
console.log('Testing Database Connection...');
```

5. نتیجه را به من بگویید

---

## 🎯 تست 3: اتصال دیتابیس

**روش تست:**

1. در Console، این کد را اجرا کنید:

```javascript
// تست اتصال به دیتابیس
const testDB = async () => {
  try {
    const { data, error } = await supabase
      .from('profiles')
      .select('*')
      .limit(1);
    
    if (error) {
      console.log('❌ خطا:', error.message);
    } else {
      console.log('✅ اتصال موفق! تعداد رکوردها:', data?.length || 0);
    }
  } catch (e) {
    console.log('❌ خطا:', e.message);
  }
};

testDB();
```

---

## 🤖 تست 4: AI (Gemini)

**روش تست:**

1. در سایت، به بخش AI بروید (اگر دارید)
2. یک پیام تستی بفرستید
3. ببینید آیا پاسخ می‌دهد

یا در Console:

```javascript
// تست AI Proxy
fetch('/api/proxy', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    action: 'generateContent',
    model: 'gemini-2.5-flash',
    data: {
      contents: [{ role: 'user', parts: [{ text: 'سلام' }] }],
      config: {}
    }
  })
})
.then(r => r.json())
.then(d => console.log('✅ AI Response:', d))
.catch(e => console.log('❌ AI Error:', e));
```

---

## 💳 تست 5: پرداخت (ZarinPal Sandbox)

**روش تست:**

1. یک محصول را به سبد خرید اضافه کنید
2. به صفحه پرداخت بروید
3. ببینید آیا به ZarinPal Sandbox هدایت می‌شوید

---

## 📊 خلاصه نتایج

| تست | وضعیت | نتیجه |
|-----|-------|-------|
| دسترسی به سایت | ✅ موفق | سایت بارگذاری شد |
| Environment Variables | ⏳ در حال تست | منتظر تست دستی |
| اتصال دیتابیس | ⏳ در حال تست | منتظر تست دستی |
| AI (Gemini) | ⏳ در حال تست | منتظر تست دستی |
| پرداخت (ZarinPal) | ⏳ در حال تست | منتظر تست دستی |

---

**لطفاً تست‌های بالا را انجام دهید و نتایج را به من بگویید!** 🌴
