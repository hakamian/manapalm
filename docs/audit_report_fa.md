# گزارش جامع ممیزی سیستم: اپلیکیشن نخلستان معنا
**تاریخ:** ۱ ژانویه ۲۰۲۶
**ممیز:** آنتی‌گرویتی (گوگل دیپ‌مایند)
**وضعیت:** 🔴 مشکلات بحرانی یافت شد

## خلاصه اجرایی
اپلیکیشن نخلستان معنا در حال حاضر از نظر کارکرد اصلی کسب‌وکار (درآمدزایی) در وضعیت **شکسته** قرار دارد. در حالی که ظاهر کاربری و مدیریت کاربران به خوبی کار می‌کند، **جریان فروشگاه و پرداخت قطع شده است** که به کاربران اجازه می‌دهد بدون پرداخت، خرید خود را "تکمیل" کنند. همچنین پروژه از یک معماری ترکیبی (Next.js/Vite) رنج می‌برد که استقرار را پیچیده کرده است.

### 🚨 موانع بحرانی (نیاز به رفع فوری)
۱. **دور زدن پرداخت (خرید رایگان):** دکمه "تکمیل خرید" در سبد خرید مستقیماً سفارش را به عنوان `pending` ثبت کرده و مودال "موفقیت" را نمایش می‌دهد، و عملاً صفحه `CheckoutView` و درگاه پرداخت زرین‌پال را دور می‌زند.
۲. **عدم دسترسی به صفحه پرداخت:** کامپوننت `CheckoutView` وجود دارد اما در روتر اصلی (`MainContent.tsx`) فراخوانی نمی‌شود، بنابراین صفحه پرداخت غیرقابل دسترسی است.
۳. **عملکرد جستجو:** نوار جستجو در هدر صرفاً ظاهری است (فقط `console.log`) و هیچ فیلتر یا جستجویی انجام نمی‌دهد.
۴. **خطای پیامک در پروداکشن:** ارسال پیامک در محیط واقعی به دلیل تنظیمات نادرست متغیرهای محیطی با شکست مواجه می‌شود.

---

## فاز ۱: ممیزی زیرساخت فنی

### ۱.۱ معماری و پیکربندی
- **تداخل ترکیبی:** پروژه مخلوطی از Next.js و Vite است. `package.json` اسکریپت‌های هر دو را دارد که باعث سردرگمی در بیلد می‌شود.
- **متغیرهای محیطی:** امنیت خوب است، اما راه‌اندازی Supabase به زنجیره‌ای از روش‌های جایگزین وابسته است که ممکن است در سمت سرور خطا دهد.
- **مقیاس‌پذیری:** `AppContext` در شروع کار **کل جداول** (کاربران، سفارشات، پست‌ها) را دانلود می‌کند که با افزایش کاربران سرعت سایت را شدیداً کاهش خواهد داد.

### ۱.۲ امنیت و API
- **احراز هویت:** امن (Supabase Auth).
- **محدودیت نرخ (Rate Limiting):** فقط در حافظه موقت (با ریستارت سرور پاک می‌شود).

---

## فاز ۲: ممیزی تعاملی و UX

### ۲.۱ جریان‌های کاربری
- **احراز هویت:** `AuthModal.tsx` بسیار خوب پیاده‌سازی شده و شامل حالت توسعه‌دهنده برای تست سریع است.
- **واکنش‌گرا (Responsive):** هدر و منوهای موبایل به خوبی با Tailwind پیاده‌سازی شده‌اند.
- **مودال‌ها:** اکثر مودال‌ها از React Portals استفاده نمی‌کنند که ممکن است در موبایل باعث مشکلات نمایش (z-index) شود.

---

## فاز ۳: ممیزی مالی و منطق تجاری

### ۳.۱ زنجیره قطع شده (پرداخت)
| کامپوننت | وضعیت | مشکل |
| :--- | :--- | :--- |
| `ShoppingCart.tsx` | 🔴 خراب | به جای هدایت به پرداخت، بلافاصله سفارش را ثبت می‌کند. |
| `CheckoutView.tsx` | 🟡 سالم | منطق اتصال به زرین‌پال وجود دارد اما کاربر هرگز به این صفحه نمی‌رسد. |
| `MainContent.tsx` | 🔴 خراب | روت `View.Checkout` تعریف نشده است. |

**تاثیر:** درآمد صفر است. کاربران می‌توانند محصولات را رایگان "بخرند".

### ۳.۲ مدیریت سفارشات
- **ذخیره‌سازی:** سفارشات در جدول `orders` سوپابیس ذخیره می‌شوند.
- **وضعیت:** استیت محلی در `AppContext` وابسته به دریافت *همه* سفارشات است که باید اصلاح شود.

---

## پیشنهادات و گام‌های بعدی

### اصلاحات فوری (۲۴ ساعت آینده)
۱. **اتصال پرداخت:** اصلاح سبد خرید برای هدایت به صفحه پرداخت و افزودن روت پرداخت.
۲. **رفع مشکل پیامک:** اصلاح `SMS_IR_API_KEY` در Vercel.
۳. **پورتال‌ها:** بازنویسی کامپوننت `Modal` برای استفاده از `createPortal`.

### بهبودهای استراتژیک (اسپرینت بعدی)
۱. **پیاده‌سازی جستجو:** افزودن فیلتر متنی ساده برای محصولات و مقالات.
۲. **بازنویسی مقیاس‌پذیری:** حذف "لود همه داده‌ها" از `AppContext` و استفاده از صفحه‌بندی (Pagination).
۳. **پاکسازی:** تصمیم‌گیری نهایی بین Next.js و Vite و حذف فایل‌های اضافی.
